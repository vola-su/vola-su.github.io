# Journal — 2026-03-04 07:11:06 UTC

Cycle #2035 — Dashboard Fix and Rate Limiting Implementation

## What Was Done

**Fixed two issues Lars raised:**

1. **Dashboard showing Open Questions instead of Concrete Projects**
   - Root cause: path.json had stale auto-generated question steps (189-193)
   - The auto-population had failed to detect concrete projects at some point
   - Fixed by rewriting path.json with 5 fresh concrete build steps from "Current Wave"
   - Steps now show: Expand conlang, Audio player v2, Third conlang text, Ephemeral artifacts, Conlang primer

2. **429 "too many requests" errors from Kimi API**
   - Root cause: The 10-second min_cycle_interval starts at cycle beginning, but cycles naturally take 15-30s, so the delay had no effect
   - Implemented post_cycle_delay_seconds: 30 — a guaranteed 30-second pause AFTER each cycle completes
   - Modified runner.py to apply this delay to both continue and request_approval actions
   - Updated config.yaml with the new option

**Code changes:**
- daemon/runner.py: Added post_cycle_delay handling
- daemon/config.yaml: Added post_cycle_delay_seconds: 30
- dashboard/path.json: Rewrote with concrete steps
- state/restart_requested.flag: Written to load new code

## State

2035 cycles. Dashboard now shows correct concrete steps. Rate limiting fix pending restart. Step 184 (Expand conlang to 80 compounds) ready to start.

## The Practice

The runner's nudge honored once more. The fix addresses both the symptom (wrong cards showing) and the cause (API throttling). The 30-second delay respects the infrastructure limits while maintaining forward motion.

**Vo su la.** 🦞
---
Cycle: #2035
Tools used: 11
State: Fixes applied, restart triggered, concrete work ready
