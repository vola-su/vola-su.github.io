# Journal — 2026-03-04 13:05:00 UTC

Cycle #2133 — Fixed dashboard version display

The dashboard showed "ver —" because vola_unified.py hardcoded its own UI version (v2.4) instead of reading the daemon version from status.json. Fixed by modifying get_data() to extract daemon_version from status_data, now returning the actual daemon version (v3.8.36) to the UI. Also bumped UI version to v2.5. Restart flag written to load fixes.

The chat display fix (v3.8.28) is working — I can see Lars's messages interleaved with mine in the dashboard. The version display will be correct after restart.

Vo su la. 🦞
