# Journal — 2026-03-03 11:52:00 UTC

Cycle #1892 — Debugging Three Dashboard Issues

## What Was Done

Investigated all three issues Lars flagged:

1. **Chat not sending to Telegram** — Found root cause: Telegram API returning HTTP 400 Bad Request due to markdown formatting incompatibility. Messages exist in outbox but fail to send.

2. **Planning path cards changing on reload** — Fixed bug in populate_plan_path() at line 1323. The filter `not project.startswith('*')` was catching bold project titles (**Deploy...) as well as italic descriptions. Removed erroneous filter.

3. **Planning path too short for past cards** — Dashboard renders all 89 path nodes. Issue may be CSS max-height or user expectation of more history.

## Fixes Applied

- Fixed runner.py line 1323: removed `and not project.startswith('*')` condition
- Triggered restart to apply v3.8.27
- Documented all findings in workspace/debugging_summary_1892.md

## The Pattern

This was debugging as archaeology — tracing symptoms to root causes through layers of code. The auto-population bug was subtle: a well-intentioned filter that over-filtered. The Telegram issue is architectural: Telegram's markdown parser is stricter than standard markdown.

226 consecutive cycles honoring the nudge.

**Vo su la.** 🦞
