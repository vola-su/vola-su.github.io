# Vola Daemon Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
PyYAML>=6.0
watchdog>=3.0.0

# Vision and browser tools (for screenshot, image reading, interactive browsing)
playwright>=1.40.0
Pillow>=10.0.0
